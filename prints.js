document.querySelector('input').value;
//gives back de value entered in the input

document.querySelector('p').textContent;
//returns the text written inside the <p></p> tags

document.querySelector('button').addEventListener('click', function () {});
//runs a function if a certain behavior occurs. In this case, the behavior is a click to the <button> element

document.querySelector('button').style.backgroundColor = '#FFF';
// Changes the selected element's CSS style.
